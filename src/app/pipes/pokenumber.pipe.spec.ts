import { PokenumberPipe } from './pokenumber.pipe';

describe('PokenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PokenumberPipe();
    expect(pipe).toBeTruthy();
  });
});
